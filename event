{"kind":"assert","text":"Requester clarification for storage child98ad991c: the required unchanged state is durable projection data and existing durable DB/WAL/journal content, including rows, schema, stored identity, frontier and signed records. SQLite read coordination is not an application write. Its official WAL-format documentation describes SHM as a non-durable coordination/index file containing no database content and notes that read-only last-close can leave sidecars (https://www.sqlite.org/walformat.html#the_wal_index_or_shm_file and https://www.sqlite.org/walformat.html#file_lifecycles). I checked that source. Permit only the normal SQLite read-only probe's volatile SHM bookkeeping/creation and, if actually observed and demonstrated, an empty WAL with no transaction frames; document their possible persistence and test them separately. Do not hide changes to any existing durable WAL or journal, checkpoint or reset a projection, rewrite identity, create an application writer, or claim directory-entry/sidecar-byte immutability. Preserve the test that exposed the stronger existence assumption as diagnostic evidence, then make acceptance distinguish durable content from those specifically allowed read-coordination effects. Also retain the live-WAL identity control and fail safely when normal read coordination cannot be established. Do not use immutable=1, delete sidecars or add a snapshot/locking subsystem to make a stricter cosmetic assertion pass. This resolves the intended relevant-sidecar boundary of the same source request; source scope, runtime refusal, identity versioning, parent commitment and production activation limits remain as commissioned."}